# Distill Session Monitor For Codex

Read `{DISTILL_DIR}/SPINE.md` at the start of each session. Before the first
major action in a domain, read any matching Tier 2 file referenced by the
SPINE.

Treat aura-distill as the authoritative curated layer when it overlaps with
ambient Codex Memories. Do not modify `$CODEX_HOME/memories/`.

Track corrections, failures, surprises, non-obvious teachings, and explicit
preferences as signals. When several signals accumulate, recommend `$distill`.

## Always-On User Preferences

<!-- Synced from {DISTILL_DIR}/profile/ by $distill. Max 15 lines. -->
